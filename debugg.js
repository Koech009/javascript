// function divide(a, b) {
//   console.log('a:', a); // Log the value of a
//   console.log('b:', b); // Log the value of b 

//   if (b === 0) {
//     console.error('Division by zero error'); // Log an error message
//     return null;
//   }
//   return a / b;
// }

// let result = divide(10, 0);
// console.log('Result:', result); // Result: null

function divide(a, b) { 
    debugger;
    if (b === 0) { 
      return null; 
    } 
    return a / b; 
  }
   
divide(10, 0); 